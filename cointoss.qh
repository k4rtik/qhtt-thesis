cointoss : {⊤} b : bit {uniform(b)}
cointoss = circ () -> do
              q <- init 0
              apply H to q
              b <- meas q
              return b

cointoss : {⊤} b : bit {uniform(b)}
cointoss = circ () -> do
              -- ⊤
              q <- init 0
              -- q =q |0⟩
              apply H to q
              -- q = 1/√2(|0⟩+|1⟩) ⇒ q = |+⟩
              x <- meas q
              -- class(x), u_x = |+⟩
              return x
